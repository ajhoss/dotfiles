" Aarons vimrc file

set ignorecase 								"ignore case when using a search pattern
set nocompatible							"makes sure vim options are used
filetype on										"checks file type to allow proper syntax highlighting
syntax on											"turns on syntax highlighting
set background=dark						"sets background color
set ruler											"puts cursor position at bottom of window
set history=700								"expanded history
set showmatch									"shows matching brackets
set relativenumber number			"line numbers on left
set cursorline                "better visibility of cursor line
set tabstop=2                 "number of spaces that a tab counts for
set colorcolumn=80            "creates vertical ruler"
